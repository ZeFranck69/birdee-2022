<section id="banner" class="banner"
	style="background-color: {{ section.background_color }};">
	<div class="banner__container banner__container--{{ section.ornaments_type }} banner__position--{{ section.layout }}">

		<div class="banner__content layout--{{ section.text_layout }}" style="max-width: {{ section.text_width }}px;">
			{% if section.logo %}
				<img src="{{ section.logo.url }}" alt="{{ section.logo.url }}" class="banner-logo"> 
			{% endif %}
			{% if section.title %}
			<h1 class="banner-content__title title-banner bold uppercase">
				{{ section.title }} 
			</h1>
			{% endif %}
			{% if section.description %}
			<div class="banner-content__description description">
				{{ section.description }} 
			</div>
			{% endif %}
			{% if section.button.title %}
			<div class="banner-content__cta-wrapper">
				<div class="banner-content__bullet">
					<a href="{{ section.button.url }}" class="cta cta--green" target="{{ section.button.target }}">
						<span class="cta__label">
							{{ section.button.title }}
						</span>	
					</a>
				</div>
			</div>
			{% endif %}
			{% if section.trustpilot %}	
			<div class="trustpilot__wrapper">
				{{ section.trustpilot }}
			</div>
			{% endif %}
		</div>
		<div class="banner__image-part" style="max-width:{{ section.image_block_width }}px;">
			{% if section.image %}
				<img src="{{ section.image.url }}" alt="{{ section.image.alt }}" class="banner__image" style="max-width:{{ section.width }}px;">
			{% endif %}
			
		</div>

	</div>
</section>

